xv6 内核运行机制实践报告
一、 实验内容说明
本次实践完成了“23级计算机科学与技术专业2026春季学期实践周任务”中定义的多个层级任务：  

1. 第一层：机制观察（已完成）
任务 1：系统调用路径跟踪：在 write 系统调用的用户层、内核入口（syscall）及实现处（sys_write）添加调试输出，追踪执行路径。  

任务 2：调度过程观察：修改 proc.c 中的 scheduler() 函数，记录进程切换日志，观察不同 PID 进程的交替执行。  

任务 3：内存分配观察：在 kalloc.c 中记录页分配情况，观察物理内存的动态分配过程。  

2. 第二层：机制理解（已完成）
任务 1：系统调用扩展：在内核中新增了 hello() 系统调用，并编写了用户层测试程序进行验证。  

任务 2：系统调用行为修改：尝试实现了复杂的 Lazy Allocation（懒加载内存分配） 机制，深入修改了 sbrk 系统调用及缺页中断处理逻辑。
二、 核心任务实现过程（以第二层任务 1 为例）
实现步骤：
内核层实现：在 sysproc.c 中编写 sys_hello(void)，利用 cprintf 在内核态打印信息。  

注册系统调用：

在 syscall.h 中分配系统调用号 SYS_hello。  

在 syscall.c 中添加外部函数声明并更新 syscalls 映射数组。  

用户层接口：

在 user.h 中定义用户态函数原型 int hello(void)。  

在 usys.S 中添加 SYSCALL(hello) 以生成汇编入口。  

编写测试程序：创建 user/hello.c，调用 hello() 并通过 Makefile 加入编译列表。  

实验运行结果截图说明：



三、 遇到的问题及解决方法
Lazy Allocation 导致的内核重映射（Remap）：

问题：在实现 Lazy Alloc 时，尝试修改 mappages 取消 panic，导致内核初始化时产生 Triple Fault 导致系统不断重启（打印 123）。  

解决：通过对 vm.c 底层逻辑的深入排查，意识到内核启动初期的映射必须严格遵循原版逻辑。最终采取恢复稳定版 vm.c 的策略，确保系统能进入 Shell 完成观察任务。  

Makefile 编译格式错误：

问题：修改 UPROGS 列表后报 recipe commences before first target。  

解决：发现是 Tab 键与空格混用以及反斜杠后多余空格导致的。通过重新使用 Tab 键对齐解决。  

环境兼容性导致的链接错误：

问题：在恢复原版代码后，exec.c 报错缺少 clearpteu 和 vmprint 定义。  

解决：在 vm.c 中根据环境版本手动补全了这两个函数的定义，成功完成了内核链接。


四、 实践心得
通过本次为期一周的 xv6 内核实践，我从最初对内核代码的“畏难”逐渐转变为能够深入源码进行功能定制。
这次经历让我对操作系统运行机制有了从抽象到具体的跨越性认识。首先，系统调用不再是一个枯燥的概念。通过手动追踪 write 的执行路径，我清晰地看到了 CPU 如何通过中断（Trap）从受限的用户态切换到高特权的内核态，并在寄存器中传递系统调用号。这种“跨越边界”的交互是操作系统安全与功能的核心。  
其次，进程调度与内存管理的观察任务给我留下了深刻印象。在控制台上不断滚动的 [SCHED] 日志让我意识到，我们流畅的使用体验背后是内核在毫秒级时间内进行的疯狂切换。
而尝试实现 Lazy Allocation 的过程虽然充满了挫折（尤其是那无限循环的 123 重启），但也逼迫我必须去读懂页表项（PTE）的结构和虚拟地址映射的本质。我意识到，内核开发中哪怕一行代码逻辑的偏差，都会导致整个系统的崩塌，这种严谨性是应用层开发难以比拟的。  
最后，虽然在实验后期遇到了环境不兼容导致的启动困难，但在反复的 make clean 与代码回滚中，我学会了如何使用 Git 记录进度、如何分析链接报错以及如何保持一颗冷静的 Debug 强心脏。正如任务书所言，这次实践不仅是技术的提升，更是对操作系统“如何运行”的一次深度致敬。
